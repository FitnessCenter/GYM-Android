<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorDefault</item>
        <item name="colorAccent">@color/colorDefault</item>

        <item name = "windowActionBar">false</item>
        <item name = "windowNoTitle">true</item>
    </style>

    <style name="SignCheckBoxTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/colorDefault</item>
        <item name="android:textColorSecondary">@color/colorLowBlack</item>
    </style>

    <style name="AppTheme.RoundedCornerMaterialButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/back_layout_shape_round</item>
    </style>

    <style name="SignEditTextStyle" parent="@style/Widget.AppCompat.EditText">
        <item name="colorControlActivated">@color/colorDefault</item>
        <item name="colorControlNormal">@color/colorLowBlack</item>
    </style>

</resources>