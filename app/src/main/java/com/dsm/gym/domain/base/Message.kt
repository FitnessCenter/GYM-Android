package com.dsm.gym.domain.base

enum class Message {
    NO_CONTENT,
    INVALID,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    UNKNOWN_ERROR,
    SERVER_ERROR,
    NETWORK_ERROR
}